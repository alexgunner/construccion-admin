<p id="notice"><%= notice %></p>

<h1>Acerca de</h1>

<%= link_to 'Registrar', new_about_path, class: "btn btn-primary" %>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descripción</th>
      <th>Imagen</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @abouts.each do |about| %>
      <tr>
        <td><%= about.name %></td>
        <td><%= about.description %></td>
        <td><%= image_tag(about.pic.url, style: "width: 300px;") %></td>
        <td><%= link_to 'Editar', edit_about_path(about) %></td>
        <td><%= link_to 'Eliminar', about, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


