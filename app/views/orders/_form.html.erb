<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :client %>
    <%= form.text_field :client, id: :order_client %>
  </div>

  <div class="field">
    <%= form.label :confirmed %>
    <%= form.text_field :confirmed, id: :order_confirmed %>
  </div>

  <div class="field">
    <%= form.label :reservedate %>
    <%= form.text_field :reservedate, id: :order_reservedate %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
