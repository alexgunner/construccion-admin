<%= form_with(model: stock, local: true) do |form| %>
  <% if stock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stock.errors.count, "error") %> prohibited this stock from being saved:</h2>

      <ul>
      <% stock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= form.hidden_field :store_id, :value => @store.id %>
  <div class="form-group">
    <%= form.label  "Producto" %>
    <%= form.collection_select :product_id, Product.order(:name), :id, :name, include_blank: true %>
  </div>

  <div class="form-group">
    <%= form.label "Variante" %>
    <%= form.grouped_collection_select :variant_id, Product.order(:name), :product_variants, :name, :id, :description, include_blank: true %>
  </div>

  <div class="field">
    <%= form.label "Cantidad" %>
    <%= form.number_field :quantity, id: :stock_quantity %>
  </div>

  <div class="field">
    <%= form.label "Mínimo de cantidad" %>
    <%= form.number_field :limit, id: :stock_limit %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
