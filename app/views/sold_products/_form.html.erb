<%= form_with(model: sold_product, local: true) do |form| %>
  <% if sold_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sold_product.errors.count, "error") %> prohibited this sold_product from being saved:</h2>

      <ul>
      <% sold_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_variant_id %>
    <%= form.number_field :product_variant_id, id: :sold_product_product_variant_id %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity, id: :sold_product_quantity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
