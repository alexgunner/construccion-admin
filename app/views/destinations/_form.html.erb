<%= form_with(model: destination, local: true) do |form| %>
  <% if destination.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(destination.errors.count, "error") %> prohibited this destination from being saved:</h2>

      <ul>
      <% destination.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label "Nombre" %>
    <%= form.text_field :name, id: :destination_name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label "Ciudad" %>
    <%= form.text_field :city, id: :destination_city, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label "País" %>
    <%= form.text_field :country, id: :destination_country, class: "form-control" %>
  </div>

  <div class="actions">
    <center>
      <%= form.submit "Aceptar", class: "btn btn-success" %>
      <%= link_to "Cancelar", "/destinos", class: "btn btn-danger" %>
    </center>
  </div>
<% end %>
