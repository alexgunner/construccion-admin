<%= form_with(model: service, local: true, html: { multipart: true }) do |form| %>
  <% if service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(service.errors.count, "error") %> prohibited this service from being saved:</h2>

      <ul>
      <% service.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label "Nombre" %>
    <%= form.text_field :title, id: :service_title, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label "Descripción" %>
    <%= form.text_area :description, id: :service_description, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label "Imagen" %>
    <%= form.file_field :pic, id: :service_pic_id, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit "Aceptar", class: "btn btn-success" %>
    <%= link_to "Cancelar", "/abouts", class: "btn btn-danger" %>
  </div>
  </div>
<% end %>
